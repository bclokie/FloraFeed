import React from 'react';

const containerStyle = {
  width: '100%',
  height: '60vh',
  margin: 'auto'
};

const center = {
  lat: 0,
  lng: 0,
};

function GridView() {
    return (
        <p>'Grid'</p>
    )
}

export default GridView;