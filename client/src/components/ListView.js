import React from 'react';

const containerStyle = {
  width: '100%',
  height: '60vh',
  margin: 'auto'
};

const center = {
  lat: 0,
  lng: 0,
};

function ListView() {
    return (
        <p>'List'</p>
    )
}

export default ListView;